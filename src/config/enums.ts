export enum UserRole {
    STUDENT = 'Student',
    ADMIN = 'Admin',
    PROFESSIOR = 'Professior',
  }

  export enum UserStatus{
    ACTIVE = 'Active',
    INACTIVE = 'Inactive',
  }

  export enum QuestionType {
    MCQ = 'MCQ',
    OPNE = 'OPEN',
  }